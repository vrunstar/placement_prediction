{
  "name": "placement-prediction-app",
  "version": "1.0.0",
  "description": "Student Placement and Salary Prediction using Machine Learning",
  "main": "app.py",
  "scripts": {
    "start": "streamlit run app.py --server.port $PORT --server.headless true --server.address 0.0.0.0"
  },
  "dependencies": {},
  "engines": {
    "python": "3.9"
  }
}